bet = document.getElementById("bet_<%= @like.bet.id %>")
bet.id = "like_<%= @like.id %>";
bet.setAttribute("data-method", "delete");
bet.setAttribute("href", "/bets/<%= @like.bet.id %>/likes/<%= @like.id %>");
bet.childNodes[1].classList.remove("card-light");
bet.childNodes[1].classList.add("card-opaque");

image = `<img height="50" id="user_<%= @like.user.id %>" class="liker-avatar" src="<%= @like.user.photo_url %>">`
cardLikers = document.getElementById("card_bet_<%= @like.bet.id %>")
cardLikers.childNodes[3].insertAdjacentHTML("beforeend", image)
